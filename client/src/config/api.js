// API configuration
// In production, this will be set from environment variable during build
// In development, it defaults to localhost
const API_URL = process.env.REACT_APP_API_URL || '/api';

export default API_URL;

